on:
  push: 
    branches:
    - main
  workflow_dispatch:
    inputs:
      name:
        type: string
        default: Colin

jobs:
  job1:
    runs-on: ubuntu-latest
    
    steps:
    - run: |
        echo "Hi there ${{ inputs.name }}"
  
    - run: |
        echo "Also hi there ${{ inputs.name }}"
  
